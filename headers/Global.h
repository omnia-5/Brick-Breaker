#ifndef GLOBAL_H
#define GLOBAL_H
#include <SFML/Graphics.hpp>
#include <iostream>

const sf::Vector2f APP_SIZE = sf::Vector2f(1280, 740);
const sf::Vector2f BRICK_SIZE = sf::Vector2f(96, 32);
const sf::Vector2f PLAYER_SIZE = sf::Vector2f(224, 32);
const sf::Vector2f COLOR_SIZE = sf::Vector2f(64, 64);

#endif // !GLOBAL_H
